# board

Puts the models from [models](../models) on the NUCLEO-H533RE.

Documented under [docs/](docs).

- [export](docs/export.md) writes one nonlinear autoencoder from a run of
  `evaluate.pc.run` out as float and int8 ONNX.
