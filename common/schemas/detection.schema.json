{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "detection.json",
  "description": "One entry per detector, the rules first, then each model as the thresholds record it. Each holds what it caught at each k, keyed by k, when k of the last `n` rows raise an alarm.",
  "type": "array",
  "prefixItems": [
    {
      "$ref": "#/$defs/rules"
    }
  ],
  "items": {
    "$ref": "#/$defs/model"
  },
  "minItems": 1,
  "$defs": {
    "rules": {
      "type": "object",
      "properties": {
        "detector": {
          "const": "rules"
        },
        "false_positive_rate": {
          "description": "The share of moving rows with no attack that the model's score alone flags. Read it against `TARGET`.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      },
      "patternProperties": {
        "^[0-9]+$": {
          "$ref": "#/$defs/at_k"
        }
      },
      "required": [
        "detector",
        "false_positive_rate"
      ],
      "additionalProperties": false
    },
    "model": {
      "$ref": "model.schema.json",
      "properties": {
        "threshold": {
          "description": "The score above which the model flags a row.",
          "type": "number"
        },
        "false_positive_rate": {
          "description": "The share of moving rows with no attack that the model's score alone flags. Read it against `TARGET`.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      },
      "patternProperties": {
        "^[0-9]+$": {
          "$ref": "#/$defs/at_k"
        }
      },
      "required": [
        "threshold",
        "false_positive_rate"
      ],
      "unevaluatedProperties": false
    },
    "at_k": {
      "description": "What the detector caught at one k.",
      "type": "object",
      "properties": {
        "found": {
          "description": "Attacks caught, of all the attacks injected.",
          "type": "integer",
          "minimum": 0
        },
        "alarms_per_hour": {
          "description": "False positive alarms per hour. An alarm is a false positive when none of its rows is attacked.",
          "type": "number",
          "minimum": 0
        },
        "caught": {
          "description": "Which attacks were caught, numbered as `attacks.json` lists them.",
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "found_worth_catching": {
          "description": "The same over the attacks that reach a row above `min_speed` and moved it by at least `moved`.",
          "type": "integer",
          "minimum": 0
        }
      },
      "required": [
        "found",
        "found_worth_catching",
        "alarms_per_hour",
        "caught"
      ],
      "additionalProperties": false
    }
  }
}
