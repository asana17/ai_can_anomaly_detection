{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "thresholds/<time>/meta.json",
  "description": "Where thresholds came from.",
  "allOf": [
    {
      "$ref": "meta.schema.json"
    }
  ],
  "properties": {
    "inputs": {
      "type": "object",
      "properties": {
        "models": {
          "description": "The `models/<time>` directory.",
          "type": "string",
          "pattern": "^models/[0-9]{8}-[0-9]{6}$"
        },
        "target": {
          "$ref": "meta.schema.json#/$defs/target"
        },
        "onnx_files": {
          "description": "The `onnx/<time>` or `quantize/<time>` directory the models scored as, or null for torch.",
          "type": [
            "string",
            "null"
          ]
        },
        "precision": {
          "description": "The precision of those ONNX files, or null for torch.",
          "enum": [
            "float",
            "int8",
            null
          ]
        }
      },
      "required": [
        "models",
        "target",
        "onnx_files",
        "precision"
      ],
      "additionalProperties": false
    },
    "scores": {
      "$ref": "meta.schema.json#/$defs/directory",
      "description": "The scores of the calibration set the thresholds were taken from."
    },
    "models": {
      "$ref": "meta.schema.json#/$defs/directory",
      "description": "The directory the models came from."
    },
    "onnx_files": {
      "$ref": "meta.schema.json#/$defs/onnx_files",
      "description": "The ONNX files the models scored as, as the scores record them. Null when the models scored in torch."
    },
    "calibration_set": {
      "$ref": "meta.schema.json#/$defs/directory",
      "description": "The calibration set the rows came from, as the scores record it."
    },
    "log_split": {
      "$ref": "meta.schema.json#/$defs/directory",
      "description": "The log split, as the scores record it."
    },
    "grid": {
      "$ref": "meta.schema.json#/$defs/directory",
      "description": "The grid, as the scores record it."
    },
    "min_speed": {
      "$ref": "meta.schema.json#/$defs/min_speed",
      "description": "The speed a row had to exceed to set a threshold."
    },
    "rows": {
      "description": "How many scored rows each threshold was taken from.",
      "type": "integer",
      "minimum": 0
    },
    "versions": {
      "$ref": "meta.schema.json#/$defs/versions",
      "description": "Python, NumPy and the platform."
    }
  },
  "required": [
    "scores",
    "models",
    "onnx_files",
    "calibration_set",
    "log_split",
    "grid",
    "min_speed",
    "rows",
    "versions"
  ],
  "unevaluatedProperties": false
}
